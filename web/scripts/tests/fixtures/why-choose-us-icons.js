/**
 * Plain-JS stand-in for web/components/about/WhyChooseUs/Icons.js (a JSX
 * module). Adapter tests only need these exports to exist; the real icon
 * components remain the production implementation.
 */
function PlaceholderIcon() {
  return null;
}

export const MissionTargetIcon = PlaceholderIcon;
export const VisionEyeIcon = PlaceholderIcon;
