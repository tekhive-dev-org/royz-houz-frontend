# Royz Houz public website — local Supabase CLI configuration.
#
# This file intentionally does not contain a remote project reference or any
# credential. Link a local checkout to the approved shared remote project with
# `supabase link --project-ref <remote-project-ref>` when instructed.
#
# web/supabase/migrations owns foundational public website and content objects.

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[inbucket]
enabled = true
port = 54324

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000/**"]

[storage]
enabled = true
file_size_limit = "50MiB"
